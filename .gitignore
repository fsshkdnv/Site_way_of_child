# Dependencies
node_modules/

# SQLite (локальная БД — не коммитить)
db/*.sqlite
db/*.sqlite-journal
db/*.sqlite-wal
db/*.sqlite-shm

# Секреты и локальная конфигурация
.env
.env.local
.env.*.local

# Логи и ОС
*.log
.DS_Store
Thumbs.db
